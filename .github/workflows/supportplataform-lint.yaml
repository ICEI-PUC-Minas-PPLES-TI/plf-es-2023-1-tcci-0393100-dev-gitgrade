name: SupportPlataform Lint

on:
  pull_request:
    branches: [master]
    paths:
      - "Codigo/supportplataform-service/**"

jobs:
  lint:
    name: SupportPlataform Lint | OS ${{ matrix.os }} - Node Version ${{ matrix.node-version }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node-version: [18.x]

    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v2
      - uses: reviewdog/action-eslint@v1
        with:
          tool_name: eslint-github-pr-review
          workdir: ./Codigo/supportplataform-service
          reporter: github-pr-review
          level: error
          github_token: ${{ github.token }}
